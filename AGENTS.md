# VCHEERS 绿厅座位预约 MVP

项目目标：用一套可运行原型验证“微信身份 → 会员编号绑定 → 查看小时余量 → 连续时段预约 → 管理员名册、容量和状态处理”的最小闭环。

## 技术栈

- React + TypeScript + Vite
- 共享领域状态机与浏览器会话内演示数据
- Vitest 覆盖容量、重叠、幂等、取消、恢复、到场补记和每日表格导出
- Playwright 覆盖用户端与管理端真实操作链路

## 约定

- IMPORTANT：唯一预约对象是 V cheers 绿厅共享座位池，不建立活动、具体座位、包场或多空间模型。
- IMPORTANT：一笔预约对应一名用户、一个座位和一段连续 1–4 小时时间，营业时间为 13:00–22:00。
- IMPORTANT：容量按日期设置，同日各小时共享每日容量；连续时段任意一小时满员则整笔失败。
- IMPORTANT：预约提交即确认，不加入人工审核、候补、支付或二维码签到。
- IMPORTANT：每笔新预约必须分别同意本地的《预约信息使用说明》和《空间使用守则》，两项默认不勾选，领域层严格校验 `privacyAccepted` / `rulesAccepted`。阅读不等于同意；成功后重置勾选，失败保留。
- IMPORTANT：新预约保存 `privacyConsent` / `rulesConsent` 的版本与确认时间，历史记录允许缺省，不追认旧占位勾选；保留 v4 会话键、预约、绑定及审计，Excel 不增加确认列。
- IMPORTANT：空间守则全文是阅读内容，不据此引入新的签到方式等范围。正式接入前须按实际主体和存储方式更新预约信息使用说明。
- IMPORTANT：会员编号必须由管理员名册导入，用户只能将有效、未被占用的编号绑定到微信身份，预约时不得自行填写编号。
- IMPORTANT：会员编号（VC-ID）固定为 6 位数字，始终按字符串保存，保留前导零；导入和绑定均校验，不接受字母。
- IMPORTANT：管理员可以补记当天或历史预约的到场、撤销误标，并取消未标记到场的预约；未来日期不能提前标记到场。用户自行取消仍限预约开始前，恢复预约仍须满足时间、余量和重叠校验。
- IMPORTANT：管理员可按预约日期下载 Excel，每笔预约一行，包含取消记录；字段固定为 VC-ID、姓名、手机号、预约日期、预约时长、是否到场。时长为数值小时，到场字段为“是 / 否 / 已取消”。
- IMPORTANT：用户端与管理端页面入口分开；正式版的登录、角色和逐接口权限必须由服务端验证。
- IMPORTANT：容量、时间窗口、归属、重叠、状态和恢复规则必须在领域层校验，不能只靠界面隐藏按钮。
- IMPORTANT：本阶段是本地可运行 Web 原型；接入微信 AppID、服务端和数据库前，不得宣称具备生产级并发与权限保障。
- 中文界面以可读和可执行为先，正文和主要控件不小于 16px。
- 全站共用暖白底、品牌紫 `#784a99` 与草绿 `#78bb2e`，使用 PDF 原矢量的共用 `BrandLogo`；不再叠加独立深绿主题。手机点击目标至少 44px，少说明文案、少嵌套卡片。
- 新增业务范围前先更新需求文档与本文件，避免把已排除功能带回首版。

## 原型限制

- `sessionStorage` 只能验证单标签页交互，不能替代服务端事务、数据库锁和唯一约束。
- 原型使用虚构姓名、手机号和会员编号，禁止录入真实个人信息。
- 当前开放演示保留原登录页面，任意绑定输入可通过 `demoAccess` 回退到虚拟演示身份。此例外仅适用于 Web 演示入口，不放宽领域层绑定、角色或预约规则，正式接入时必须移除。
- `/admin` 仅用于演示管理界面；正式版本必须由服务端按微信身份逐接口鉴权。
